#include <bits/stdc++.h>

#define f first
#define s second
#define ll long long
#define MAXN 100005
#define fast ios::sync_with_stdio(NULL); cin.tie(0); cout.tie(0);
#define clear_arr(a) memset(a, 0, sizeof(a));

using namespace std;

int a[MAXN];

void solve(){
    
    
    cout << "\n";
    memset(a, 0, sizeof(a));
}

int main() {
    fast

    int t;
    cin >> t;

    while (t--) {
        solve();
    }

    return 0;
}

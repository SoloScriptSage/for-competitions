#include <bits/stdc++.h>
 
#define f first
#define s second
#define ll long long
#define MAXN 100005
 
using namespace std;
 
int a[MAXN];
 
void solve(){
    cout << "\n";
    memset(a, 0, sizeof(a)); 
}
 
int main() {
    ios::sync_with_stdio(NULL);
    cin.tie(0);
    cout.tie(0);
 
    int t;
    cin >> t;
 
    while (t--) {
        solve();
    }
 
    return 0;
}
